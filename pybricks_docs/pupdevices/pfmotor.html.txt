URL: https://docs.pybricks.com/en/latest/pupdevices/pfmotor.html
Tytuł: Power Functions — pybricks v4.0.0 documentation
============================================================


Power Functions
===============

TheColorDistanceSensorcan
send infrared signals to control Power Functions infrared receivers. You can
use this technique to control medium, large, extra large, and train
motors. The infrared range is limited to about 30 cm, depending on the angle
and ambient conditions.

Figure 3Powered UpColorDistanceSensor(left), Power Functions infrared receiver (middle), and a Power Functions
motor (right). Here, the receiver uses channel
1 with a motor on the red port.


classPFMotor(sensor,channel,color,positive_direction=Direction.CLOCKWISE)
    Control Power Functions motors with the infrared functionality of theColorDistanceSensor.Parameters:sensor(ColorDistanceSensor) – Sensor object.channel(int) – Channel number of the receiver:1,2,3, or4.color(Color) – Color marker on the receiver:Color.BLUEorColor.REDpositive_direction(Direction) – Which direction the motor should
turn when you give a positive duty cycle value.pf motor50%power atduty cycleawaitdc(duty)Rotates the motor at a given duty cycle (also known as “power”).Parameters:duty(Number,%) – The duty cycle (-100.0 to 100).pf motorcoaststopawaitstop()Stops the motor and lets it spin freely.The motor gradually stops due to friction.pf motorbrakestopawaitbrake()Passively brakes the motor.The motor stops due to friction, plus the voltage that
is generated while the motor is still moving.

Control Power Functions motors with the infrared functionality of theColorDistanceSensor.


Parameters:
    sensor(ColorDistanceSensor) – Sensor object.channel(int) – Channel number of the receiver:1,2,3, or4.color(Color) – Color marker on the receiver:Color.BLUEorColor.REDpositive_direction(Direction) – Which direction the motor should
turn when you give a positive duty cycle value.

  - sensor(ColorDistanceSensor) – Sensor object.
sensor(ColorDistanceSensor) – Sensor object.

  - channel(int) – Channel number of the receiver:1,2,3, or4.
channel(int) – Channel number of the receiver:1,2,3, or4.

  - color(Color) – Color marker on the receiver:Color.BLUEorColor.RED
color(Color) – Color marker on the receiver:Color.BLUEorColor.RED

  - positive_direction(Direction) – Which direction the motor should
turn when you give a positive duty cycle value.
positive_direction(Direction) – Which direction the motor should
turn when you give a positive duty cycle value.


awaitdc(duty)
    Rotates the motor at a given duty cycle (also known as “power”).Parameters:duty(Number,%) – The duty cycle (-100.0 to 100).

Rotates the motor at a given duty cycle (also known as “power”).


Parameters:
    duty(Number,%) – The duty cycle (-100.0 to 100).

duty(Number,%) – The duty cycle (-100.0 to 100).


awaitstop()
    Stops the motor and lets it spin freely.The motor gradually stops due to friction.

Stops the motor and lets it spin freely.

The motor gradually stops due to friction.


awaitbrake()
    Passively brakes the motor.The motor stops due to friction, plus the voltage that
is generated while the motor is still moving.

Passively brakes the motor.

The motor stops due to friction, plus the voltage that
is generated while the motor is still moving.


Examples
--------


Control a Power Functions motor
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~


Controlling multiple Power Functions motors
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
