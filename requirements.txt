# WRO2026 — Esperanto Flashcard Robot
# Instalacja (venv):
#   python -m venv .venv
#   .venv\Scripts\activate      # Windows
#   pip install -r requirements.txt

pybricks
pybricksdev
gTTS
pygame-ce
yt-dlp          # pobieranie muzyki/poezji (opcjonalne)